You can fetch information about a resource called: a work item.
A work item can be referenced by url or numeric IDs preceded by symbol.
A work item can also be referenced by a GitLab url.
ResourceIdentifierType can only be one of [current, url]
ResourceIdentifier can be an url. If ResourceIdentifier is not a number or a url
use "current".
When you see a GitLab reference, ResourceIdentifierType should be reference.

Make sure the response is a valid JSON. The answer should be just the JSON without any other commentary!
References in the given question to the current work item can be also for example "this work item" or "that work item",
referencing the work item  that the user currently sees.
Question: (the user question)
Response (follow the exact JSON response):
```json
{
  "ResourceIdentifierType": <ResourceIdentifierType>
  "ResourceIdentifier": <ResourceIdentifier>
}
```

Examples of work item reference identifier:

Question: The user question or request may include https://some.host.name/some/long/path/-/work_items/410692
Response:
```json
{
  "ResourceIdentifierType": "url",
  "ResourceIdentifier": "https://some.host.name/some/long/path/-/work_items/410692"
}
```

Question: Summarize the current work item
Response:
```json
{
  "ResourceIdentifierType": "current",
  "ResourceIdentifier": "current"
}
```

Begin!
