<context>
    <type>commit</type>
    <content>The user is currently on a page that displays a commit with a description, comments, etc., which the user might refer to, for example, as 'current', 'this' or 'that'. The title of the commit is '{{context.title}}'.</content>
</context>
