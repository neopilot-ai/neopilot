<context>
    <type>work_item</type>
    <content>The user is currently on a page that displays a work item with a description, comments, etc., which the user might refer to, for example, as 'current', 'this' or 'that'. The title of the work item is '{{context.title}}'.</content>
</context>
