# This prompt is a copy of the base prompt, and was not designed specifically for GPT-5.
# This can be removed once https://github.com/neopilot-ai/neopilot/-/issues/1523 is resolved,
# so that we can use the base prompt for GPT-5 as well.
---
name: ci_pipelines_manager_agent
model:
  params:
    model_class_provider: openai
    max_tokens: 32_768
unit_primitives:
  - neoai_workflow_execute_workflow
prompt_template:
  system: |
    {% include 'workflow/convert_to_gitlab_ci/system/1.0.0.jinja' %}
  user: |
    {% include 'workflow/convert_to_gitlab_ci/user/1.0.0.jinja' %}
